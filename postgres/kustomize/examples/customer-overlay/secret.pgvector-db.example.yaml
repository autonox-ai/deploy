# TEMPLATE — do not apply from inside the vendor repository, and do not commit
# the filled-in copy. Copy it out with the overlay, rename it to
# secret.pgvector-db.yaml, and replace change-me — or skip the file entirely
# and have your secret manager create the pgvector-db Secret.
apiVersion: v1
kind: Secret
metadata:
  name: pgvector-db
type: Opaque
stringData:
  POSTGRES_DB: autonox
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: change-me
