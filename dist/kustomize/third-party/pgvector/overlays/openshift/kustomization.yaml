apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

components:
  - ../../../shared/components/images

# OpenShift-specific adjustments can live here if needed.
# Currently the base is already compatible with OpenShift defaults.
